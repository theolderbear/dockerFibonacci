import React from 'react';
import {link} from 'react-router-dome';

export default () => {
    return (
        <div>
            Im some other page!
        <Link to="/">Go back home</Link>
        </div>

    );
};